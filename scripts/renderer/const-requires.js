// These constants are avaiable in all other script files.
// Seems weird, I know, but maybe manageable while there's only a few
const ipc = require('electron').ipcRenderer;
const $ = require('jQuery');